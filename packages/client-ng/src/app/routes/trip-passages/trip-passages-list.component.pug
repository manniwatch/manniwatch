mat-nav-list(*ngIf="hasPassages(); else noPassagesBlock",role="list")
    app-trip-passages-list-item(*ngFor="let passageItem of passages",[passage]="passageItem")
ng-template(#noPassagesBlock)
    h3 No Passages