mat-nav-list(*ngIf="hasDepartures()",role="list")
    mwlr-stop-departure-list-item(*ngFor="let passage of departures",[departure]="passage")
.no-departures(*ngIf="!hasDepartures()")
    h4 No Departures
