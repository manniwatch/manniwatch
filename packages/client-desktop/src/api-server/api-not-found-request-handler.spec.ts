/*!
 * Source https://github.com/manniwatch/trapeze
 */

import { expect } from 'chai';
import 'mocha';

describe('api-server/api-not-found-request-handler', (): void => {
    describe('createApiNotFoundRequestHandler', (): void => {
        it('should test', (): void => {
            expect(true).to.equal(true);
        });
    });
});
